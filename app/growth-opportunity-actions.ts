'use server';

import { revalidatePath } from 'next/cache';
import { redirect } from 'next/navigation';
import { getCurrentOrganization } from '@/lib/supabase/org';
import { buildGrowthOpportunity } from '@/lib/hunters/business/growth-routing';
import { deriveWhatsappCandidate } from '@/lib/hunters/business/selective-enrichment';
import type { DiscoveredBusiness } from '@/lib/hunters/business/types';

type CachedBusiness = { id:string; name:string; country_code:string; city:string|null; category:string|null; google_place_id:string|null; official_website:string|null; phone:string|null; international_phone:string|null; whatsapp:string|null; formatted_address:string|null; google_maps_uri:string|null; google_rating:number|null; google_user_rating_count:number|null; google_business_status:string|null };
const toDiscovered=(row:CachedBusiness):DiscoveredBusiness=>({sourceType:'google_places',sourceId:row.google_place_id??row.id,googlePlaceId:row.google_place_id??undefined,name:row.name,countryCode:row.country_code,city:row.city??undefined,category:row.category??undefined,officialWebsite:row.official_website??undefined,phone:row.phone??undefined,internationalPhone:row.international_phone??undefined,whatsapp:row.whatsapp??deriveWhatsappCandidate(row.international_phone,row.phone,row.country_code),formattedAddress:row.formatted_address??undefined,googleMapsUri:row.google_maps_uri??undefined,rating:row.google_rating==null?undefined:Number(row.google_rating),userRatingCount:row.google_user_rating_count==null?undefined:Number(row.google_user_rating_count),businessStatus:row.google_business_status??undefined,retrievedAt:new Date().toISOString()});

export async function routeCachedGrowthOpportunities(){const ctx=await getCurrentOrganization(true);let destination='/hunters/growth-opportunities';try{const{data,error}=await ctx.supabase.from('businesses').select('id,name,country_code,city,category,google_place_id,official_website,phone,international_phone,whatsapp,formatted_address,google_maps_uri,google_rating,google_user_rating_count,google_business_status').eq('organization_id',ctx.organizationId).not('google_place_id','is',null).limit(500);if(error)throw error;let routed=0,skipped=0,contactReady=0,paidNextActions=0;for(const row of(data??[]) as CachedBusiness[]){const business=toDiscovered(row);if(String(business.businessStatus??'').toUpperCase()!=='OPERATIONAL'){skipped+=1;continue;}const opportunity=buildGrowthOpportunity(business);const p=opportunity.personalization;if(p.cheapestNextAction==='CONTACT_READY')contactReady+=1;if(p.nextActionCanSpendMoney)paidNextActions+=1;const{error:upsertError}=await ctx.supabase.from('growth_opportunities').upsert({organization_id:ctx.organizationId,business_id:row.id,service_region:opportunity.region,sales_lane:opportunity.lane,website_class:opportunity.websiteClass,website_score:opportunity.websiteScore,local_content_score:opportunity.localContentScore,ai_content_score:opportunity.aiContentScore,overall_sales_score:opportunity.overallSalesScore,content_check_status:opportunity.contentCheckStatus,recommended_services:opportunity.recommendedServices,routing_reasons:opportunity.reasons,contactability_score:p.contactabilityScore,need_score:p.needScore,service_fit_score:p.serviceFitScore,revenue_potential_score:p.revenuePotentialScore,personalization_priority_score:p.personalizationPriorityScore,personalization_fingerprint:p.fingerprint,offer_bundle:p.offerBundle,recommended_angle:p.recommendedAngle,message_hooks:p.messageHooks,social_check_eligible:p.socialCheckEligible,cheapest_next_action:p.cheapestNextAction,next_action_reason:p.nextActionReason,next_action_can_spend_money:p.nextActionCanSpendMoney,routed_at:new Date().toISOString(),updated_at:new Date().toISOString()},{onConflict:'organization_id,business_id'});if(upsertError)throw upsertError;routed+=1;}const{error:auditError}=await ctx.supabase.from('audit_logs').insert({organization_id:ctx.organizationId,actor_type:'USER',actor_id:ctx.userId,action:'ROUTE_CACHED_GROWTH_OPPORTUNITIES',entity_type:'growth_opportunity',entity_id:ctx.organizationId,after_data:{routed,skipped,contactReady,paidNextActions,providerCalls:0,llmCalls:0,socialApiCalls:0,reviewsFetched:false,outreachTriggered:false,personalizationMode:'DETERMINISTIC_ZERO_COST'}});if(auditError)throw auditError;destination=`/hunters/growth-opportunities?routed=${routed}&skipped=${skipped}&contactReady=${contactReady}`;}catch(error){const message=error instanceof Error?error.message.slice(0,200):'Cached growth routing failed';destination=`/hunters/growth-opportunities?error=${encodeURIComponent(message)}`;}revalidatePath('/hunters/growth-opportunities');revalidatePath('/hunters');redirect(destination);}
