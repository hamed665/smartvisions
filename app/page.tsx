import Link from 'next/link';
import { getCurrentOrganization } from '@/lib/supabase/org';
export const dynamic='force-dynamic';

export default async function HomePage(){const {supabase,organizationId}=await getCurrentOrganization();const [{data:leads},{data:controls},{count:approvalCount},{data:integrations},{data:conversations}]=await Promise.all([supabase.from('leads').select('status').eq('organization_id',organizationId),supabase.from('system_controls').select('*').eq('organization_id',organizationId).maybeSingle(),supabase.from('conversation_messages').select('id',{count:'exact',head:true}).eq('organization_id',organizationId).eq('requires_approval',true).eq('status','APPROVAL_REQUIRED'),supabase.from('integration_connections').select('status,enabled').eq('organization_id',organizationId),supabase.from('sales_conversations').select('stage,requires_human,unread_count').eq('organization_id',organizationId)]);const l=leads??[],c=conversations??[],ints=integrations??[];const count=(...s:string[])=>l.filter(x=>s.includes(x.status)).length;const metrics=[['New Leads',count('NEW')],['Qualified',count('QUALIFIED','READY_TO_CONTACT')],['Contacted',count('CONTACTED')],['Replies',count('REPLIED','INTERESTED','HOT','HUMAN','WON')],['HOT',count('HOT')],['Won',count('WON')],['Needs Human',c.filter(x=>x.requires_human).length],['Approvals',approvalCount??0]];return <div><div className="headerRow"><div><h1>Control Center</h1><p className="muted">Live production overview and operator shortcuts.</p></div><span className={`status ${controls?.global_kill_switch?'dangerStatus':''}`}>{controls?.global_kill_switch?'GLOBAL STOP':controls?.shadow_mode?'Shadow Mode':'Autonomous Mode'}</span></div><section className="quickActions"><Link href="/campaigns">+ Create Campaign</Link><Link href="/leads">Manage Leads</Link><Link href="/conversations">Open Inbox</Link><Link href="/approvals">Review Approvals</Link><Link href="/messages">Edit Messages</Link><Link href="/system">Safety Controls</Link></section><section className="grid">{metrics.map(([label,value])=><div className="card" key={String(label)}><div className="muted">{label}</div><div className="value">{value}</div></div>)}</section><section className="twoCol"><div className="panel"><h2>Runtime safety</h2><div className="healthList"><span>Global kill switch <strong>{controls?.global_kill_switch?'ON':'OFF'}</strong></span><span>Email <strong>{controls?.email_paused?'PAUSED':'ENABLED'}</strong></span><span>WhatsApp AI <strong>{controls?.whatsapp_ai_paused?'PAUSED':'ENABLED'}</strong></span><span>AI agents <strong>{controls?.agents_paused?'PAUSED':'ENABLED'}</strong></span><span>Monthly budget <strong>${controls?.monthly_budget_usd??20}</strong></span></div></div><div className="panel"><h2>Integration readiness</h2><div className="healthList"><span>Ready <strong>{ints.filter(i=>i.status==='READY').length}</strong></span><span>Not configured <strong>{ints.filter(i=>i.status==='NOT_CONFIGURED').length}</strong></span><span>Degraded / error <strong>{ints.filter(i=>['DEGRADED','ERROR'].includes(i.status)).length}</strong></span><span>Enabled connections <strong>{ints.filter(i=>i.enabled).length}</strong></span></div><Link className="textLink" href="/integrations">Open integrations →</Link></div></section></div>}
