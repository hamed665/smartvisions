import { updatePrice } from '@/app/control-center-actions';
import { createPrice } from '@/app/extended-actions';
import { getCurrentOrganization } from '@/lib/supabase/org';
export const dynamic='force-dynamic';

export default async function PricingPage(){const{supabase,organizationId,role}=await getCurrentOrganization();const[{data:priceData},{data:services},{data:markets}]=await Promise.all([supabase.from('service_prices').select('id,service_id,country_code,currency,price,minimum_price,max_auto_discount_pct,max_discount_with_approval_pct').eq('organization_id',organizationId).order('country_code').order('service_id'),supabase.from('services').select('id,name,enabled').eq('organization_id',organizationId).eq('enabled',true).order('name'),supabase.from('market_settings').select('country_code,currency,enabled').eq('organization_id',organizationId).eq('enabled',true).order('country_code')]);const prices=priceData??[];const editable=role==='OWNER';return <div><div className="headerRow"><div><h1>Pricing</h1><p className="muted">Live country-specific prices and autonomous discount boundaries.</p></div><span className="status">{prices.length} price rules</span></div><div className="settingsList">{prices.map(row=><form action={updatePrice} className="settingsRow pricingRow" key={row.id}><input type="hidden" name="id" value={row.id}/><div><strong>{row.service_id}</strong><span className="muted smallText">{row.country_code} · {row.currency}</span></div><label>Price<input type="number" step="0.01" name="price" defaultValue={row.price} disabled={!editable}/></label><label>Minimum<input type="number" step="0.01" name="minimum_price" defaultValue={row.minimum_price} disabled={!editable}/></label><label>Auto discount %<input type="number" step="0.1" min="0" max="100" name="max_auto_discount_pct" defaultValue={row.max_auto_discount_pct} disabled={!editable}/></label><label>Approval discount %<input type="number" step="0.1" min="0" max="100" name="max_discount_with_approval_pct" defaultValue={row.max_discount_with_approval_pct} disabled={!editable}/></label><button disabled={!editable}>Save</button></form>)}</div>{editable?<section className="panel settingsCreate"><h2>Add market price</h2><form action={createPrice} className="settingsGrid"><label>Service<select name="service_id">{(services??[]).map(s=><option value={s.id} key={s.id}>{s.name}</option>)}</select></label><label>Market<select name="country_code">{(markets??[]).map(m=><option value={m.country_code} key={m.country_code}>{m.country_code}</option>)}</select></label><label>Currency<select name="currency">{(markets??[]).map(m=><option value={m.currency} key={`${m.country_code}-${m.currency}`}>{m.currency}</option>)}</select></label><label>Price<input type="number" step="0.01" name="price" required/></label><label>Minimum<input type="number" step="0.01" name="minimum_price" required/></label><label>Auto discount %<input type="number" step="0.1" name="max_auto_discount_pct" defaultValue="5"/></label><label>Approval discount %<input type="number" step="0.1" name="max_discount_with_approval_pct" defaultValue="10"/></label><button>Add price</button></form></section>:null}</div>}
