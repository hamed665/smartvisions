import { createAutomationRule, updateAutomationRule } from '@/app/management-actions';
import { getCurrentOrganization } from '@/lib/supabase/org';
export const dynamic='force-dynamic';
export default async function AutomationsPage(){const {supabase,organizationId,role}=await getCurrentOrganization();const {data}=await supabase.from('automation_rules').select('*').eq('organization_id',organizationId).order('priority',{ascending:false});const rows=data??[];const editable=role==='OWNER';return <div><div className="headerRow"><div><h1>Automation Rules</h1><p className="muted">Event-driven sales workflow rules. High-risk actions still pass approval policy checks.</p></div><span className="status">{rows.filter(r=>r.enabled).length} active</span></div><div className="settingsList">{rows.map(r=><form action={updateAutomationRule} className="settingsRow" key={r.id}><input type="hidden" name="id" value={r.id}/><div><strong>{r.name}</strong><span className="muted smallText">{r.trigger_key} → {r.action_key}</span></div><label>Priority<input type="number" min="0" max="100" name="priority" defaultValue={r.priority} disabled={!editable}/></label><label className="toggleLabel"><input type="checkbox" name="enabled" defaultChecked={r.enabled} disabled={!editable}/> Enabled</label><button disabled={!editable}>Save</button></form>)}</div>{editable?<section className="panel settingsCreate"><h2>Create automation</h2><form action={createAutomationRule} className="settingsGrid"><label>Name<input name="name" required placeholder="Hot lead demo"/></label><label>Trigger<select name="trigger_key"><option>LEAD_QUALIFIED</option><option>HOT_LEAD</option><option>POSITIVE_REPLY</option><option>NO_REPLY_48H</option><option>DEMO_APPROVED</option><option>PAYMENT_INTENT</option></select></label><label>Action<select name="action_key"><option>GENERATE_PREVIEW</option><option>SEND_FOLLOWUP</option><option>CREATE_OPERATOR_BRIEF</option><option>HANDOFF_HUMAN</option><option>PAUSE_AUTOMATION</option><option>MARK_HOT</option></select></label><label>Priority<input type="number" name="priority" defaultValue="50" min="0" max="100"/></label><button>Create</button></form></section>:null}</div>}
