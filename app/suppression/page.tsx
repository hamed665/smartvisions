import { addSuppression } from '@/app/management-actions';
import { deleteSuppression } from '@/app/extended-actions';
import { getCurrentOrganization } from '@/lib/supabase/org';
export const dynamic='force-dynamic';
export default async function SuppressionPage(){const {supabase,organizationId,role}=await getCurrentOrganization();const {data}=await supabase.from('suppression_list').select('*').eq('organization_id',organizationId).order('created_at',{ascending:false}).limit(200);const rows=data??[];const editable=role==='OWNER';return <div><div className="headerRow"><div><h1>Suppression & DNC</h1><p className="muted">Global no-contact protection for emails, phones and domains.</p></div><span className="status">{rows.length} blocked</span></div>{editable?<section className="panel"><form action={addSuppression} className="settingsGrid"><label>Email<input name="email"/></label><label>Phone<input name="phone"/></label><label>Domain<input name="domain"/></label><label>Reason<input name="reason" placeholder="Manual DNC"/></label><button>Add block</button></form></section>:null}<div className="tableWrap"><table className="dataTable"><thead><tr><th>Email</th><th>Phone</th><th>Domain</th><th>Reason</th><th>Source</th><th>Added</th><th></th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.email||'—'}</td><td>{r.phone||'—'}</td><td>{r.domain||'—'}</td><td>{r.reason}</td><td>{r.source}</td><td>{new Date(r.created_at).toLocaleString()}</td><td>{editable?<form action={deleteSuppression}><input type="hidden" name="id" value={r.id}/><button className="ghostButton">Remove</button></form>:null}</td></tr>)}</tbody></table></div></div>}
