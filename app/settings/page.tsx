import { updateOrganizationSettings } from '@/app/management-actions';
import { getCurrentOrganization } from '@/lib/supabase/org';
export const dynamic='force-dynamic';
export default async function SettingsPage(){const {supabase,organizationId,role}=await getCurrentOrganization();const {data}=await supabase.from('organization_settings').select('*').eq('organization_id',organizationId).maybeSingle();const editable=role==='OWNER';return <div><div className="headerRow"><div><h1>Settings</h1><p className="muted">Organization identity, operator preferences and default communication behavior.</p></div><span className="status">{role}</span></div><section className="panel"><form action={updateOrganizationSettings} className="settingsGrid"><label>Brand name<input name="brand_name" defaultValue={data?.brand_name??'Smart Visions'} disabled={!editable}/></label><label>Operator language<select name="operator_language" defaultValue={data?.operator_language??'fa'} disabled={!editable}><option value="fa">Persian</option><option value="en">English</option><option value="ar">Arabic</option></select></label><label>Default customer language<select name="default_customer_language" defaultValue={data?.default_customer_language??'en'} disabled={!editable}><option value="en">English</option><option value="ar">Arabic</option><option value="fa">Persian</option></select></label><label>Notification email<input type="email" name="notification_email" defaultValue={data?.notification_email??''} disabled={!editable}/></label><button disabled={!editable}>Save settings</button></form></section></div>}
