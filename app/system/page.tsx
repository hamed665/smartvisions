import { updateApprovalRule } from '@/app/control-center-actions';
import { updateSystemControls } from '@/app/management-actions';
import { getCurrentOrganization } from '@/lib/supabase/org';
export const dynamic='force-dynamic';

export default async function SystemPage(){const {supabase,organizationId,role}=await getCurrentOrganization();const [{data:ruleData},{data:controls}]=await Promise.all([supabase.from('approval_rules').select('id,action_key,requires_approval,config').eq('organization_id',organizationId).order('action_key'),supabase.from('system_controls').select('*').eq('organization_id',organizationId).maybeSingle()]);const rules=ruleData??[];const editable=role==='OWNER';return <div><div className="headerRow"><div><h1>System & Safety</h1><p className="muted">Runtime automation safety, budgets, emergency controls and approval boundaries.</p></div><span className={`status ${controls?.global_kill_switch?'dangerStatus':''}`}>{controls?.global_kill_switch?'KILL SWITCH ON':'Running'}</span></div><section className="panel dangerPanel"><h2>Runtime controls</h2><p className="muted">These values are live database controls. Agents and dispatchers must honor them before every autonomous action.</p><form action={updateSystemControls} className="settingsGrid"><label className="toggleLabel"><input type="checkbox" name="global_kill_switch" defaultChecked={controls?.global_kill_switch??false} disabled={!editable}/> Global kill switch</label><label className="toggleLabel"><input type="checkbox" name="shadow_mode" defaultChecked={controls?.shadow_mode??true} disabled={!editable}/> Shadow mode</label><label className="toggleLabel"><input type="checkbox" name="email_paused" defaultChecked={controls?.email_paused??false} disabled={!editable}/> Pause email</label><label className="toggleLabel"><input type="checkbox" name="whatsapp_ai_paused" defaultChecked={controls?.whatsapp_ai_paused??false} disabled={!editable}/> Pause WhatsApp AI</label><label className="toggleLabel"><input type="checkbox" name="agents_paused" defaultChecked={controls?.agents_paused??false} disabled={!editable}/> Pause all AI agents</label><label>Monthly AI/infra budget USD<input type="number" min="0" step="1" name="monthly_budget_usd" defaultValue={controls?.monthly_budget_usd??20} disabled={!editable}/></label><button disabled={!editable}>Save runtime controls</button></form></section><section className="panel"><h2>Approval boundaries</h2><p className="muted">Routine low-risk answers should remain autonomous. Mark only actions that genuinely need owner review.</p></section><div className="settingsList">{rules.map(r=><form action={updateApprovalRule} className="settingsRow" key={r.id}><input type="hidden" name="id" value={r.id}/><div><strong>{r.action_key}</strong><span className="muted smallText">Production approval policy</span></div><label className="toggleLabel"><input type="checkbox" name="requires_approval" defaultChecked={r.requires_approval} disabled={!editable}/> Requires approval</label><button disabled={!editable}>Save</button></form>)}</div></div>}
